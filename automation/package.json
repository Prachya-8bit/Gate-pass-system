{
  "name": "gatepass-epro-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node --env-file=.env epro-sync.mjs",
    "sync:dry": "node --env-file=.env epro-sync.mjs --dry-run"
  },
  "dependencies": {
    "playwright": "^1.45.0"
  }
}
