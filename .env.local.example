# Local development — copy to .env.local (Next.js loads this automatically).
# Relative path works in both WSL and Windows as long as you run
# `npm run dev` from the project root.

DATABASE_URL="file:/mnt/d/Code/factory-gate-pass-permit/prisma/dev.db"
JWT_SECRET="dev-only-secret-change-in-production"
