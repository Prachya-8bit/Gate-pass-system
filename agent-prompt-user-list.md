# Prompt สำหรับ AI Agent — หน้ารายชื่อผู้ใช้ + รีเซ็ตรหัสผ่าน

## งาน

เพิ่มหน้าจัดการบัญชีผู้ใช้ในระบบ SYS Gate Pass เพื่อให้ admin เห็นว่า username แต่ละรายการเป็นของใคร และรีเซ็ตรหัสผ่านให้ได้เมื่อผู้ใช้ลืมรหัส

**ปัญหาที่กำลังแก้:** ตอนนี้ `app/api/users/route.ts` มีแค่ `POST` (สร้างบัญชี) เมื่อ contractor ลืมรหัส admin ทำได้อย่างเดียวคือสร้างบัญชีใหม่ ทำให้มีบัญชีซ้ำสะสมเรื่อยๆ

## บริบทโปรเจกต์

- Next.js 14+ App Router, TypeScript
- PostgreSQL (Neon) ผ่าน Prisma — dev ใช้ branch `dev` ผ่าน `.env.local`
- Auth: bcrypt + JWT ใน HTTP-only cookie ชื่อ `gp_token`
- `lib/auth.ts` มี `getSession(request)` คืน `{ id, credential, role }` หรือ `null`
- Styling: **inline styles จาก design token `gDS` ใน `components/atoms.tsx` เท่านั้น**
- UI ภาษาไทยทั้งหมด

---

## สิ่งที่ต้องทำ (4 ข้อ เท่านี้)

### 1. Schema

เพิ่ม field เดียวใน model `User` ที่ `prisma/schema.prisma`:

```prisma
contactName String?   // ชื่อผู้ใช้งานจริง เพื่อให้ admin รู้ว่าบัญชีนี้ของใคร
```

nullable เพราะบัญชีที่มีอยู่แล้วยังไม่มีค่านี้

จากนั้นรัน `npx prisma migrate dev --name add_user_contact_name`

### 2. `GET /api/users`

เพิ่ม handler ในไฟล์ `app/api/users/route.ts` ที่มีอยู่แล้ว (อย่าสร้างไฟล์ใหม่ อย่าแตะ `POST` เดิม)

- ตรวจ `getSession(request)` — ถ้าไม่มี session หรือ `role !== 'admin'` ให้คืน 403 พร้อมข้อความไทย
- คืนรายการผู้ใช้: `id`, `credential`, `contactName`, `role`, `createdAt`
- เรียงตาม `createdAt` จากใหม่ไปเก่า

**ข้อบังคับด้านความปลอดภัย — ห้ามพลาด:**
ต้องใช้ `select: { ... }` ระบุ field ที่ต้องการเองเท่านั้น
**ห้าม**เรียก `prisma.user.findMany()` เปล่าแล้วมาลบ `password` ทีหลังด้วย JavaScript — ถ้าพลาด bcrypt hash จะหลุดออกทาง API

### 3. `PATCH /api/users/[id]`

สร้างไฟล์ใหม่ `app/api/users/[id]/route.ts`

รองรับ 2 อย่าง:

**ก) รีเซ็ตรหัสผ่าน** — รับ `{ password: string }`
- ตรวจความยาวอย่างน้อย 8 ตัวอักษร ไม่ผ่านคืน 400 พร้อมข้อความไทย
  - **หมายเหตุความไม่สอดคล้อง (รู้อยู่แล้ว ไม่ต้องแก้):** `POST /api/users` เดิมบังคับขั้นต่ำ 6 ตัวอักษรตอนสร้างบัญชี ส่วนรีเซ็ตรอบนี้ตั้งไว้ที่ 8 ตัวอักษร เป็นการตัดสินใจตั้งใจให้รีเซ็ตเข้มกว่า — **ห้ามแก้เกณฑ์ของ `POST` เดิมให้ตรงกัน** (อยู่นอกขอบเขต ห้ามแตะ `POST` เดิมตามที่ระบุด้านบน)
- hash ด้วย `bcryptjs` — ให้ import แบบเดียวกับที่ใช้อยู่แล้วใน `app/api/users/route.ts` คือ `import bcrypt from 'bcryptjs'` แล้วเรียก `bcrypt.hashSync(password, 10)` **ฝั่ง server เท่านั้น** — client ต้องไม่ hash อะไรทั้งสิ้น
- ห้ามเก็บรหัส plaintext ลง database และห้ามส่งรหัสกลับมาใน response

**ข) แก้ชื่อผู้ใช้งาน** — รับ `{ contactName: string }`
จำเป็นเพราะบัญชีเดิมมีค่าเป็น null admin ต้องเติมเองได้

เงื่อนไขร่วม:
- admin only เหมือนข้อ 2
- response ต้องไม่มี field `password` (ใช้ `select` เช่นกัน)

### 4. UI — ส่วนใหม่ใน `components/AdminFlow.tsx`

**หมายเหตุ:** `AdminFlow.tsx` ปัจจุบันไม่มี pattern แท็บอยู่เลย — เป็น `GCard` เรียงต่อกันแนวตั้ง (KPI → สรุปรายบริษัท → ตารางรายการ → ฟอร์มสร้างบัญชี) และ `components/atoms.tsx` ไม่มี component แท็บให้ใช้ ดังนั้น**ห้ามสร้าง component แท็บใหม่** (จะขัดกับกติกา "ห้ามสร้าง component หน้าตาใหม่ที่ไม่เข้าชุดกับของเดิม" ด้านล่าง) — ให้เพิ่มเป็น **`GCard` ใหม่อีกก้อนหนึ่ง** ต่อท้ายในหน้า ตามแพทเทิร์นเดิมของไฟล์นี้ ใช้หัวข้อ `<h3>` ว่า "จัดการบัญชีผู้ใช้" เหมือนก้อนอื่นๆ

**เกี่ยวกับฟอร์ม "สร้างบัญชีผู้ใช้ใหม่" ที่มีอยู่แล้ว (บรรทัดท้ายไฟล์):** ให้ย้ายฟอร์มนี้เข้าไปอยู่ใน `GCard` ก้อนใหม่ก้อนเดียวกัน (ไว้เหนือหรือใต้ตารางรายชื่อก็ได้) เพื่อให้การจัดการบัญชีทั้งหมดอยู่รวมกันที่เดียว — ย้ายตำแหน่งเท่านั้น ห้ามแก้ logic หรือ field ของฟอร์มเดิม

**ตาราง** คอลัมน์: ชื่อผู้ใช้งาน · Username · บทบาท · วันที่สร้าง · จัดการ

- ถ้า `contactName` เป็น null แสดงปุ่ม/ลิงก์ "เพิ่มชื่อ" แทนช่องว่าง
- `role` แสดงด้วย `Badge` — admin ใช้สี amber, contractor ใช้สี blue
- คอลัมน์จัดการ: ปุ่ม `Btn` variant `secondary` ข้อความ "รีเซ็ตรหัส"

**Modal รีเซ็ตรหัส:**
- ช่องกรอกรหัสใหม่ (`InpBox`) + ยืนยันรหัสอีกครั้ง
- กดยืนยัน → เรียก PATCH → **แสดงรหัสที่ตั้งไว้บนหน้าจอครั้งเดียว** พร้อมข้อความว่าให้แจ้งผู้ใช้ทางโทรศัพท์ และเมื่อปิดหน้าต่างแล้วจะดูซ้ำไม่ได้
- มีปุ่มคัดลอกรหัส

**ข้อกำหนดหน้าตา:**
- คงความกว้าง shell ของ admin ที่ `max-width: 880px`
- ใช้ `GCard`, `Btn`, `Badge`, `InpBox` จาก `components/atoms.tsx` เท่านั้น — ห้ามสร้าง component หน้าตาใหม่ที่ไม่เข้าชุดกับของเดิม
- สี ระยะห่าง มุมโค้ง เงา ใช้จาก token `gDS` ทั้งหมด
- ข้อความทุกจุดรวมถึง error message เป็นภาษาไทย

---

## ขอบเขต — ห้ามทำเกินนี้

หัวข้อต่อไปนี้เคยถูกพิจารณาแล้วและ **ตัดสินใจว่ายังไม่ทำในรอบนี้** ห้ามเพิ่มเข้ามาเอง ห้ามเสนอเป็น "ของแถม" ห้ามเขียนเผื่อไว้:

- ❌ ห้ามเพิ่ม field `active` / ฟังก์ชันปิดใช้งานบัญชี / soft delete
- ❌ ห้ามแตะ `app/api/auth/login/route.ts` หรือ `lib/auth.ts`
- ❌ ห้ามแตะ `components/ContractorFlow.tsx` หรือฟอร์มลงทะเบียน
- ❌ ห้ามแตะ model `Record` หรือ field `createdBy`
- ❌ ห้ามเพิ่ม audit log / ตาราง log ใดๆ
- ❌ ห้ามเพิ่ม field `company` ใน User
- ❌ ห้ามทำ email reset link / ห้ามเพิ่ม mail provider
- ❌ ห้ามเขียน script ย้ายข้อมูลหรือรวมบัญชีซ้ำ
- ❌ ห้ามลบผู้ใช้ (`prisma.user.delete`) ไม่ว่ากรณีใด
- ❌ ห้ามเพิ่ม Tailwind หรือ CSS framework ใดๆ
- ❌ ห้ามใช้ `localStorage` หรือ `sessionStorage`
- ❌ ห้ามเปลี่ยนข้อความภาษาไทยที่มีอยู่เดิม
- ❌ ห้าม refactor โค้ดส่วนอื่นที่ไม่เกี่ยวกับ 4 ข้อข้างบน

ถ้าระหว่างทำพบว่ามีปัญหาอื่นในโค้ด **ให้เขียนบอกไว้ท้ายงาน อย่าแก้เอง**

---

## เกณฑ์ตรวจรับ

- [ ] `npx tsc --noEmit` ผ่าน ไม่มี error
- [ ] `npx prisma migrate dev` ผ่าน migration ใหม่มีไฟล์เดียว
- [ ] login เป็น admin → เห็นแท็บใหม่ → เห็นรายชื่อบัญชีทั้งหมด
- [ ] เปิด DevTools → Network → response ของ `GET /api/users` **ไม่มี field `password`**
- [ ] login เป็น contractor แล้วเรียก `GET /api/users` ตรงๆ → ได้ 403
- [ ] เรียก `GET /api/users` โดยไม่ login → ได้ 401 หรือ 403 ไม่ใช่ 200
- [ ] รีเซ็ตรหัสแล้ว logout → login ด้วยรหัสใหม่ได้ และรหัสเก่าใช้ไม่ได้
- [ ] ตั้งรหัสสั้นกว่า 8 ตัว → ขึ้น error ภาษาไทย ไม่บันทึก
- [ ] เติม `contactName` ให้บัญชีที่ยังว่าง แล้วค่าแสดงในตารางถูกต้อง
- [ ] หน้าจอใหม่กว้างไม่เกิน 880px และหน้าตาเข้าชุดกับแท็บเดิม

## ผลลัพธ์ที่ต้องส่ง

1. รายการไฟล์ที่แก้/สร้าง พร้อมสรุปสั้นๆ ว่าแต่ละไฟล์ทำอะไร
2. คำสั่งที่ต้องรันเพื่อ apply migration
3. รายการปัญหาที่พบระหว่างทางแต่ **ไม่ได้แก้** (ถ้ามี)
